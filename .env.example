# Google Sheet
GOOGLE_SHEET_ID=1yrR83W15kKevye87ksYnELUY68i_j4_kIIY_gu0bAWU
GOOGLE_SHEET_GID=0
# Column letter that contains order numbers (A = first column)
ORDER_COLUMN=A
# Optional header row to skip (1 = first row is header)
HEADER_ROWS=1

# Path to Google service account JSON (share the sheet with the service account email)
GOOGLE_SERVICE_ACCOUNT_FILE=./credentials/unimap-put-in-storage-google-service-account.json

# UniUni
UNIMAP_URL=https://dispatch.uniuni.com/main

# Browser profile (keeps Microsoft login session)
BROWSER_PROFILE_DIR=./.browser-profile
